Django==5.2.8
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.4.0
python-decouple==3.8
Pillow==12.0.0
# psycopg2-binary intentionally omitted for Week 1-4 (SQLite default).
# Uncomment + pip install once you switch DB_ENGINE=postgres (see README Section 6).
# psycopg2-binary==2.9.9

# --- Week 3: real-time chat ---
channels==4.1.0
channels-redis==4.2.0
daphne==4.1.2

# --- Week 4: background jobs (broadcast, cooldown, backup reassignment) ---
celery==5.4.0
redis==5.0.8

# --- Week 5: masked calling ---
twilio==9.3.5
